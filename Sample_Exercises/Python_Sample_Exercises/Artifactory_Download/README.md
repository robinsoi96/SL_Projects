#Using Python to download content from artifactory

Command to execute:

```
python3 Download.py --link <Artifactory File Link> --user <username> --password <password> --path <local download path with filename>
```